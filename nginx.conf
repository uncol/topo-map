server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location = / {
        try_files /vanilla-index.html =404;
    }

    location / {
        try_files $uri $uri/ /vanilla-index.html;
    }
}
